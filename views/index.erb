<script>
 $( function() {
   var availableTags = [
    <% @actors.each do |actor| %>
      "<%= actor.name %>",
    <% end %>
   ];
   $( ".tags" ).autocomplete({
     source: availableTags,
     minLength: 3,
   });
 } );
 </script>


<div class="jumbotron">
  <h1>Six Degrees</h1>
  <div class="row">
    <div class="col-sm-4">
      <div class="slide">
        <img class="mySlides" src="../img/smile.jpg">
        <img class="mySlides" src="../img/hair.jpg">
      </div>
    </div>
    <div class="col-sm-4">
      <div class="slide2">
        <img class="mySlides" src="../img/hi.jpg">
        <img class="mySlides" src="../img/head.jpg">
      </div>
    </div>
    <div class="col-sm-4">
      <div class="slide3">
        <img class="mySlides" src="../img/nic.jpeg">
        <img class="mySlides" src="../img/namaste.jpg">
      </div>
    </div>
  </div>
</div>
<div class="container">
  <h4>Choose two actors and see how they are connected:</h4>


  <div class="ui-widget">
    <form action="/degrees" method="post">
      <div class="form-group">
        <label for="test">test</label>
        <input type="text" name="actor_one" class="tags" "form-control">
        <label for="test2">test</label>
        <input type="text" name="actor_two" class="tags">
    </div>
    <button class="btn btn-info" type="submit">test2</button>
    </form>
  </div>



  <h2>Current movies included in database:</h2>
  <h4>Click on a movie name to access their inventory:</h4>
  <% if @movies.any? %>
    <% @movies.each do |movie| %>
      <ul>
        <li><a href="/movies/<%= movie.id %>"><%= movie.title %></a></li>
      </ul>
    <% end %>
  <% else %>
    <p>There are no movies in the database yet.</p>
  <%end%>


  <h2>Current actors included in database:</h2>
  <h4>Click on a actor name to access their information:</h4>
  <% if @actors.any? %>
    <% @actors.each do |actor| %>
      <ul>
        <li><a href="/actors/<%= actor.id %>"><%= actor.name %> <%= actor.id %></a></li>
      </ul>
    <% end %>
  <% else %>
    <p>There are no actors in the database yet.</p>
  <%end%>
  </div>
